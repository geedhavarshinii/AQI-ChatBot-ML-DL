export async function predictAQI(latitude, longitude) {
    // This is a placeholder. In a real application, you would call your API here.
    return Math.floor(Math.random() * 200); // Returns a random AQI between 0 and 200 for demonstration
  }